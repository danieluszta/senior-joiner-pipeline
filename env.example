# Copy to .env (gitignored). The code the agent builds must load this
# explicitly — don't rely on the shell having sourced it. Never commit real values.

DATABASE_URL=            # Supabase Postgres connection string
BLITZ_API_KEY=           # Blitz API key (people search, company enrich, emails)
OPENAI_API_KEY=          # for the gpt-4o-mini gates
